/**
 * Shared extension-wide constants.
 *
 * Import from here rather than duplicating literal strings across modules.
 */

/** Base URL of the Sesam Management Portal. Used for user-facing links and portal API calls. */
export const DEFAULT_PORTAL_URL = "https://portal.sesam.io";
