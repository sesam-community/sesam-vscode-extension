{
  "name": "sesam-ts",
  "private": true,
  "version": "0.0.0",
  "description": "sesam-ts monorepo — @sesam/core, @sesam/cli, and the Sesam VS Code extension",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "pnpm": "^9.0.0"
  }
}
